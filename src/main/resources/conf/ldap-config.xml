<?xml version="1.0" encoding="UTF-8"?>  
<beans xmlns="http://www.springframework.org/schema/beans"  
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context.xsd">

	<bean id="contextSourceTarget" class="org.springframework.ldap.core.support.LdapContextSource">
		<!-- <property name="pooled" value="false" /> -->
		<property name="url" value="${LDAP_URL}" />
		<property name="base" value="${LDAP_BASE}" />
		<property name="userDn" value="${LDAP_USER}" />
		<property name="password" value="${LDAP_PASSWORD}" />

		<!-- Place JNDI environment properties here. -->
		<!-- <property name="baseEnvironmentProperties"> <map> Three seconds is 
			an eternity to users. <entry key="com.sun.jndi.ldap.connect.timeout" value="3000" 
			/> <entry key="com.sun.jndi.ldap.read.timeout" value="3000" /> Explained 
			at http://docs.oracle.com/javase/jndi/tutorial/ldap/security/auth.html <entry 
			key="java.naming.security.authentication" value="simple" /> </map> </property> -->
	</bean>
	<bean id="contextSource"
		class="org.springframework.ldap.transaction.compensating.manager.TransactionAwareContextSourceProxy">
		<constructor-arg ref="contextSourceTarget" />
	</bean>
	<bean id="ldapTemplate" class="org.springframework.ldap.core.LdapTemplate">
		<constructor-arg ref="contextSource" />
	</bean>
	<!-- serrvice注入 -->
	<bean name="ldapService" class="com.catlbattery.alm.service.impl.LdapServiceImpl">
		<property name="ldapTemplate">
			<ref bean="ldapTemplate" />
		</property>
		<property name="equalsFilter" value="sAMAccountName" />
	</bean>

</beans>  