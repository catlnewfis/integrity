<?xml version="1.0" encoding="UTF-8"?>  
<beans xmlns="http://www.springframework.org/schema/beans"  
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context.xsd">  

    <bean id="integrityFactory" class="com.catlbattery.alm.connect.IntegrityFactory">
  		<property name="host" value="${INTEGRITY_HOSTNAME}" />
	    <property name="port" value="${INTEGRITY_PORT}" />
  		<property name="secure" value="${INTEGRITY_SECURE}" />
  		<property name="majorVersion" value="${MAJOR_VERSION}" />
	    <property name="minorVersion" value="${MINOR_VERSION}" />
	    <property name="initSession" value="${INIT_SESSION}" />
	    <property name="maxSessionSize" value="${MAX_SESSION_SIZE}" />
	    <property name="waitTimes" value="${WAIT_TIMES}" />
	    <property name="lazyCheck" value="${LAZY_CHECK}" />
	    <property name="periodCheck" value="${PERIOD_CHECK}" />
	    <property name="maxCmdRunners" value="${MAX_CMD_RUNNERS}" />
	     <property name="userConfig">
            <value>classpath:properties/integrity.properties</value>
        </property>  
        <property name="userKeyPrefix" value="multi_" />  
	</bean>

</beans>  